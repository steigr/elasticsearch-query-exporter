apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "elasticsearch-query-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "elasticsearch-query-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "elasticsearch-query-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
