apiVersion: v1
kind: Service
metadata:
  name: {{ include "elasticsearch-query-exporter.fullname" . }}
  labels:
    {{- include "elasticsearch-query-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "elasticsearch-query-exporter.selectorLabels" . | nindent 4 }}
